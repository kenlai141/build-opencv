git clone https://github.com/davisking/dlib.git

cd dlib
mkdir build; cd build; cmake .. -DDLIB_USE_CUDA=1 -DUSE_AVX_INSTRUCTIONS=1
cmake --build .

cd ..
sudo pip3 install setuptools
sudo python3 setup.py install
